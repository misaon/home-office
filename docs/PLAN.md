# Home Office — Implementation Plan

**Status:** plan confirmed by the owner on 2026-09-06 ("yes", plus D11–D14). **Phases 0–5 complete** (2026-09-06); Phase 6 (intake: postman, mailbox, GitHub Issues) is next. Companion docs: `ARCHITECTURE.md` (how), `STACK.md` (with what, verified versions), `CONVENTIONS.md` (rules). Agents working on this repo: read `AGENTS.md` first, then this file; append to § Log when you finish a task.

- **D15 (2026-09-06) Browser in the sandbox.** Agents do frontend work, so the single agent image carries headless Chromium (Alpine `chromium` 142), Node 22 + npm, Bun 1.4.2 (musl, hash-pinned) and two preinstalled, pinned MCP servers: `@playwright/mcp` 0.0.80 (drive pages, snapshots, screenshots) and `chrome-devtools-mcp` 1.8.0 (performance, network, console). Both drive the distribution's Chromium with `--no-sandbox` (the container is the sandbox) and isolated profiles; screenshots land in `/tmp/browser`. Enabled for work and review sessions (`config.browser.enabled`), never for triage. Owner chose one image over a per-project variant or a sidecar; container defaults rise to 3 GB RAM and 2048 pids.
- **D16 (2026-09-06) Desktop shell wiring.** `apps/desktop` is a Bun workspace member and a Hutch project (`packageManager: "bun"`, Electrobun 2.0.1; devkit projected into the git-ignored `.hutch/`). The Bun main process runs `@ho/daemon` in-process and the window loads the **same UI bundle the daemon serves** over `http://127.0.0.1:<port>/` (one code path for desktop and browser); the launch token reaches the webview through a `preload` statement that parks it in `sessionStorage` (never in a URL). If a `ho daemon` is already healthy the app attaches to it instead of starting its own; a pid lock in `HO_HOME/desktop.lock` keeps one desktop instance. Quit = veto `before-quit`, stop sessions and GC, then quit. Packaged builds carry `Resources/app/ho/` (UI bundle, sprites, image contexts with a **prebuilt ho-runner** and the role packs, Drizzle migrations), assembled by `bun run desktop:prepare`; the daemon takes `resourcesRoot` explicitly and skips compiling the runner when the sources are absent. Logs go to `HO_HOME/logs/desktop.log`; PATH is widened with the usual Homebrew/Docker locations because Finder-launched apps do not see them.

## 1. Vision (restated requirements)

A monorepo **local multi-agent harness**: a desktop app (macOS Apple Silicon first, unsigned download from GitHub Releases) shows a pixel-art office in the LimeZu _Modern Office_ style. Employees are AI agents (name, appearance, gender, model, effort, base prompt, specialisation). A boss agent receives work from a chat panel on the right (or, later, from a postman delivering GitHub issues to a mailbox) and delegates to workers; workers hand work to each other **visibly** (walk over, hand it off). Idle agents wander, drink coffee, use the toilet, smoke, relax, sleep; emotions show in bubbles. Each agent session runs in an isolated Alpine-based Docker container; containers, caches and volumes are cleaned up aggressively. Everything is provider-agnostic (Claude first via **subscription**, later API keys, other CLIs, local LLMs) and sandbox-agnostic (Docker now, GCP later) through shared interfaces. Multiple repositories are first-class (one floor per project). Runs headless as a CLI on a server too. TypeScript everywhere on Bun, strictest typing and linting, ultra-modern stack, token- and hardware-frugal, secure. No tests in this phase.

## 2. Decisions (with the owner, 2026-09-06)

| #   | Decision                                                                                                                            | Rationale                                                                                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | **Electrobun 2.0** desktop shell, `build.mainProcess: "bun"`, unsigned macOS builds                                                 | Pure TS/Bun, main process is the daemon (no sidecar), typed RPC, delta updater. Tauri 2.11 is the documented fallback if Electrobun blocks a spike. |
| D2  | **Docker Engine API directly** via Bun `fetch({ unix })`; Swarm mode unused                                                         | Ephemeral per-session containers, precise lifecycle/GC, zero deps; Swarm services fit long-running replicas, not tasks.                             |
| D3  | **One floor per project** (tabs) + shared Lobby floor + elevator                                                                    | Owner's choice; scales with many projects; cross-project handoffs become elevator trips.                                                            |
| D4  | **Isolated clone per task**, results pushed as `ho/*` branches by a `git-bridge` helper; agent containers never mount the host repo | Safe parallelism, no host FS exposure, portable to cloud.                                                                                           |
| D5  | **Claude Code CLI headless** (`stream-json`) with `claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN` (Team plan)                      | Official subscription path for scripts/CI; Agent SDK is explicitly not allowed with claude.ai login.                                                |
| D6  | **ACP-shaped `AgentRuntime`** interface                                                                                             | Lets Gemini CLI, OpenCode (local LLMs) and Codex plug in via one generic ACP adapter later.                                                         |
| D7  | **Runner connects back** (compiled Bun `ho-runner` dials the daemon's WebSocket)                                                    | Avoids Docker attach/hijack; token never in image/container/exec metadata; same for cloud.                                                          |
| D8  | **ECC** skills vendored per role (small subsets), **RTK** hook in the image                                                         | Quality without paying for 286 skills of context on every turn.                                                                                     |
| D9  | oxlint + tsgolint + oxfmt + knip on TypeScript 7                                                                                    | Type-aware linting on the native compiler; strictest practical setup.                                                                               |
| D10 | Tests deferred to Phase 8 (owner)                                                                                                   | Prototype speed; design for testability anyway.                                                                                                     |
| D11 | License **MIT**                                                                                                                     | Owner confirmation 2026-09-06.                                                                                                                      |
| D12 | Default boss model **`opus`** at `high`; workers `sonnet`/`medium`, reviewer `sonnet`/`high`, clerk `haiku`/`low`                   | Owner confirmation; `opus` is the Team default alias.                                                                                               |
| D13 | Defaults: global concurrency **2**, task-volume retention **24 h**, idle stop 10 min                                                | Owner confirmation.                                                                                                                                 |
| D14 | `.claude/` settings inside cloned project repos are **not loaded** into sessions (per-project opt-in later)                         | Owner confirmation; keeps context lean and prevents repo-supplied hooks from running.                                                               |

## 3. Non-goals for now

Code signing/notarization · Windows/Linux packaging · cloud sandbox provider · Jira connector · multi-user/auth beyond a local token · tests · usage credits (owner pays nothing beyond the Team subscription).

## 4. Phases

Each task has an id (`P<phase>.<n>`), an **Acceptance** line and a **Validate** hint. Order within a phase is a suggestion; dependencies are noted.

### Phase 0 — Foundations and de-risking spikes (Complexity: Medium)

Goal: a green monorepo skeleton and hard evidence that the five risky assumptions hold on this machine.

- [x] **P0.1 Repo scaffold.** Root `package.json` (workspaces `apps/*`, `packages/*`, `catalog` with every version from `STACK.md`), `bunfig.toml` (`linker="isolated"`, `exact=true`), `tsconfig.base.json` + project references, `.oxlintrc.json` (type-aware), `.oxfmtrc.json`, `knip.json`, `.editorconfig`, `.gitignore`, `.gitattributes`, `LICENSE` (owner to confirm MIT), `AGENTS.md` + `CLAUDE.md` (`@AGENTS.md`), README. Scripts: `check` (tsc + lint + fmt:check + knip), `lint`, `fmt`, `dev`, `build`. Verify `tsc -b` works on TS 7.0; fall back to `bun run --filter '*' typecheck`.
      Acceptance: `bun install && bun run check` passes on an empty workspace with one placeholder package. Validate: `bun run check`.
- [x] **P0.2 CI.** `.github/workflows/ci.yml` on `ubuntu-latest`: `oven-sh/setup-bun`, frozen install, `bun run check`. Acceptance: green run on `main`.
- [x] **P0.3 Spike S1 — Claude Code headless in Alpine arm64.** `images/agent/Dockerfile` (alpine 3.22, `apk add bash curl libgcc libstdc++ ripgrep git`, Claude Code from the official apk repo with key verification, non-root `agent`, `USE_BUILTIN_RIPGREP=0`). Run `claude -p --output-format stream-json …` with `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token`, model `haiku`, `--max-turns 2`. Record cold-start time, `system/init` payload, `result.usage`, and confirm `--permission-mode bypassPermissions` works as non-root and `--bare` does **not** authenticate.
      Acceptance: JSONL result with `session_id` and usage; a follow-up turn via `--resume` works. Validate: `bun spikes/s1-claude-alpine.ts`.
- [x] **P0.4 Spike S2 — Electrobun + Bun main process + PixiJS.** `bunx electrobun init`, set `mainProcess: "bun"`, prove `bun:sqlite`, `Bun.serve` WebSocket and `fetch({ unix })` work in the main process; webview renders a Pixi 8 scene (WebGPU or WebGL in WKWebView) at 3× pixel scale with `roundPixels`; `hutch electrobun build:release` with `mac.codesign:false` produces a DMG/zip that opens after `xattr -cr`.
      Acceptance: unsigned app launches, shows 60 sprites moving, WS round-trip from webview to Bun. If blocked: document and switch D1 to Tauri.
- [x] **P0.5 Spike S3 — Docker Engine API from Bun + runner-connects-back.** Create network/volume/container via API 1.55 over the unix socket (`fetch({ unix })`), start, `exec` a detached `ho-runner` (compiled `bun-linux-arm64-musl`) that dials `ws://host.docker.internal:<port>` with a one-time token, relays a child process's stdout/stdin as JSONL, then remove everything by label.
      Acceptance: end-to-end echo through the container in < 3 s; `docker ps -a --filter label=ho.managed` empty afterwards.
- [x] **P0.6 Spike S4 — oRPC over WebSocket on Bun.** Contract with a query, a mutation and an event-iterator subscription; server in Bun, client in the webview. Acceptance: typed client compiles, 1,000 events stream without leaks.
- [x] **P0.7 Spike S5 — git-bridge.** Alpine+git image; clone host repo (RO mount) into a volume, commit inside a second container, push back to `ho/spike` via a RW bridge run. Acceptance: branch appears in the host repo; host working tree untouched.
- [x] **P0.8 Sprite pipeline.** `assets/README.md` with the asset request list (below), naming convention, `assets/pack.ts` (Bun.Image or a small packer) producing PixiJS spritesheet JSON. Acceptance: one packed sheet loads in S2.

**Exit criteria:** S1–S5 pass (or documented pivots), `bun run check` green, CI green, Log updated. Spikes are deleted or promoted into packages in Phase 1–2.

### Phase 1 — Protocol, core domain, store, daemon skeleton, CLI (Complexity: Medium)

- [x] **P1.1 `@ho/protocol`.** Branded IDs, Zod schemas for Project/Agent/Task/Session/Handoff/ChatMessage/MailItem, the `DomainEvent` discriminated union, oRPC contract (`projects.*`, `agents.*`, `tasks.*`, `sessions.*`, `chat.*`, `events.subscribe`, `usage.*`, `resources.*`, `system.health`), MCP tool input/output schemas. Acceptance: contract compiles and `knip` is clean.
- [x] **P1.2 `@ho/core`.** Ports (§5 of ARCHITECTURE), task state machine (pure transition function with exhaustive checks), scheduler (queue, per-agent/global concurrency, budgets, rate-limit pause), handoff rules, `Result` type, `Clock`. Acceptance: pure package, zero runtime imports of Bun/DOM.
- [x] **P1.3 `@ho/store`.** Drizzle schema (`events`, `projections_*`), migrations (`drizzle-kit`), `EventStore` on `bun:sqlite` WAL, projections rebuilt from events on startup, snapshot on shutdown. Acceptance: append/read/subscribe; rebuild of 100k events < 2 s.
- [x] **P1.4 `@ho/secrets`.** Keychain via `security` CLI, file fallback, Zod-validated config loader for `~/.config/home-office/config.json`. Acceptance: round-trip set/get/delete.
- [x] **P1.5 `@ho/daemon` skeleton.** Composition root, pino logging, `Bun.serve` with oRPC WS handler + bearer token, static UI serving stub, health endpoint, graceful shutdown, GC job scaffold. Acceptance: `ho daemon` starts, `ho system health` answers over oRPC.
- [x] **P1.6 `apps/cli`.** `ho daemon`, `ho project add|list|remove`, `ho agent add|list|edit`, `ho task create|list|show`, `ho chat "<text>"`, `ho tail` (event stream), `ho doctor`. Compiled with `bun build --compile`. Acceptance: create a project and an agent, see events in `ho tail`.

### Phase 2 — Sandbox and Claude Code runtime (Complexity: High)

- [x] **P2.1 `@ho/sandbox-docker`.** `SandboxProvider` over Engine API 1.55: images (build via `docker buildx build` subprocess with content-hash label; everything else via API), volumes, network `ho-agents` (ICC off), create with hardening (§11), start/stop/remove, `exec` detached, stats, prune by label/age, health (`/version`, `/system/df`). Acceptance: full lifecycle from the CLI; `ho doctor --prune` leaves nothing labelled `ho.managed` behind.
- [x] **P2.2 Agent image.** Multi-stage Dockerfile: RTK built for aarch64-musl (stage `rust:alpine`), Claude Code via apk (pinned version), git, `ho-runner`, `/opt/ho/plugins/<role>` from `@ho/agent-kit`, `/etc/ho/claude-settings.json` (hooks: RTK; env; `autoCompactWindow`), `/etc/ho/mcp.json` (HO MCP server, `${HO_MCP_URL}`/`${HO_SESSION_TOKEN}` expansion). Non-root. Acceptance: image < 400 MB, `claude --version` works as `agent`.
- [x] **P2.3 `ho-runner`.** Dials the daemon WS with the one-time token, receives `{ argv, env, secrets }`, spawns `claude`, relays JSONL both ways with back-pressure, forwards exit code, heartbeats. Acceptance: kill/interrupt semantics (SIGINT ends the turn, SIGTERM exits 143) verified.
- [x] **P2.4 `@ho/runtime-claude-code`.** Command assembly (§6), stream-json codec (Zod), normalisation to `RuntimeEvent`, usage capture from `result`, `api_retry` → `rate_limited`, error codes (`authentication_failed`, `billing_error`, …), resume tokens. Acceptance: a task brief produces a `result` and `usage.recorded` events; a forced 429 path pauses the scheduler.
- [x] **P2.5 git-bridge integration.** Clone on session start, push on report, branch naming, retention GC, optional `gh pr create` on the host (off by default). Acceptance: end-to-end "fix a typo" task produces branch `ho/<slug>` in the host repo.
- [x] **P2.6 Usage and resources projections.** Tokens per agent/task/day, cache-hit share, containers/volumes/disk snapshot every 60 s. Acceptance: `ho usage` and `ho resources` print live numbers.

### Phase 3 — Boss, handoffs, review, budgets (Complexity: High)

- [x] **P3.1 HO MCP server.** Streamable HTTP on the daemon (`@modelcontextprotocol/sdk`), per-session bearer scope, tools `ho_delegate`, `ho_handoff`, `ho_report`, `ho_ask_human`, `ho_task_status`, `ho_list_agents`; every call → event. Acceptance: tools appear in `system/init.mcp_servers` and are callable from a session.
- [x] **P3.2 Boss persona and roster prompt.** Role prompt (≤60 lines), roster rendering (agents, skills, load, projects), decomposition guidance, protocol enforcement (must use tools). Acceptance: a chat brief yields ≥1 delegated task with acceptance criteria.
- [x] **P3.3 Worker and reviewer protocol.** Role prompts, `ho_report` schema, review loop with round cap, back-to-work findings. Acceptance: a task passes through `in_progress → review → done` with two distinct agents.
- [x] **P3.4 Handoff engine.** `handoff.started` → wait for `office.handoff_delivered` (or timeout) → start target session with brief + branch; cross-project handoff moves the branch reference, never files. Acceptance: visible handoff gating verified with the UI in Phase 4 (until then a mocked completion).
- [x] **P3.5 Budgets, concurrency, rate limits.** Global/agent concurrency, per-task turn/time caps, `rate_limited` pause with `retryAt`, "sleep" state for agents, resume after reset. Acceptance: with `maxConcurrent=1`, two tasks serialise; a simulated rate limit pauses and resumes.
- [x] **P3.6 Chat.** Human ↔ boss streaming messages, task cards, `ho_ask_human` prompts with reply routing to the waiting session. Acceptance: `ho chat` round-trip with a question and an answer.

### Phase 4 — Office simulation and UI (Complexity: High)

- [x] **P4.0 Asset import.** `bun run assets:import`: reads `assets/inbox/**/*@8x.png` (see `assets/README.md`), downsamples by sampling each 8×8 block centre (tolerant to generator blur), keys out `#FF00FF` when no alpha, slices `_strip<N>` files into `_f<n>.png` frames and `autotile3x3` sheets into tiles, validates sizes (characters 32×32, tiles 16×16, furniture multiples of 16) and writes native PNGs to `assets/src/`. Pure Bun (PNG decode/encode with `Bun.inflateSync`/`deflateSync`, no native deps). Acceptance: a generated 256×256 strip becomes correctly sliced 32×32 frames that render crisply at 3× in S2.
- [x] **P4.1 `@ho/sim` core.** Floor templates (Lobby + project template JSON), grid + A*, seat/anchor reservations, elevator, 20 Hz deterministic tick, seeded RNG. Acceptance: headless run of 10 agents for 10 simulated minutes with zero collisions/deadlocks.
- [x] **P4.2 Behaviours and emotions.** Needs model, idle behaviours (coffee, restroom, smoke, relax, chat pairs, sleep), event→intent mapping (§9), emotion derivation. Acceptance: table of event → visible behaviour verified in a debug harness.
- [x] **P4.3 `@ho/ui` shell.** React 19 + Compiler, Tailwind 4, Zustand store fed by `events.subscribe`, oRPC client with the launch token, layout (canvas left, chat right, panels). Acceptance: connects to a running daemon and renders live agent statuses.
- [x] **P4.4 Pixi renderer.** Layers (baked tiles, furniture, characters z-sorted, bubbles), 3× pixel scale, spritesheet loading from `assets/`, 30 fps cap, pause on hidden, floor switching, building strip. Acceptance: 12 agents at < 5% CPU on the dev MacBook when idle.
- [x] **P4.5 Panels.** Board (per project columns), Agent inspector (normalised event log, branch, usage), Usage, Resources (prune button), Settings (projects, agents with appearance picker, budgets, auth token). Acceptance: every mutation in Settings round-trips to config and events.
- [x] **P4.6 Handoff and boss animations wired to Phase 3.** `office.handoff_delivered` emitted by the sim through RPC. Acceptance: a real delegation shows the boss walking/elevator/handing over before the worker's container starts.

### Phase 5 — Desktop packaging and first run (Complexity: Medium)

- [x] **P5.1 `apps/desktop`.** Electrobun config (`mainProcess: "bun"`, `mac.codesign:false`, `copy` of the built UI bundle), main process starts `@ho/daemon`, Electrobun RPC for window/menu/dialogs/open-external and passing the launch token to the webview, single-instance lock, quit → graceful daemon shutdown (stop sessions, GC). Acceptance: `hutch electrobun dev` runs the full app.
- [x] **P5.2 First-run wizard.** Docker check (API version ≥ 1.44, disk), image build with progress, token entry (`claude setup-token` instructions + paste, stored in Keychain), smoke session with `haiku`, default agents (boss + 2 workers + reviewer) and floor templates. Acceptance: fresh `HO_HOME` reaches a working office in < 10 min including image build.
- [x] **P5.3 Release workflow.** `release.yml` on tag: `macos-26` runner, `bun install`, build UI, `hutch electrobun build:release`, upload DMG/zip + checksums to GitHub Releases with `xattr -cr` instructions in release notes. Acceptance: downloaded artifact runs on the owner's Mac.
- [x] **P5.4 Server/CLI parity.** Daemon serves the UI bundle over HTTP; `ho daemon --ui` prints the URL with the token in a local file, not the URL. Acceptance: same UI works in a browser against `ho daemon`.

### Phase 6 — Intake: postman, mailbox, GitHub Issues (Complexity: Medium)

- [ ] **P6.1 `IntakeConnector` + `@ho/intake-github`.** Poll `gh issue list --json …` per project (labels/filters configurable), dedupe by id, `mail.received` events, acknowledge by comment/label and PR link on completion. Acceptance: a new issue becomes a task within one poll interval.
- [ ] **P6.2 Postman and clerk choreography.** Postman spawn/entrance/mailbox drop; clerk (or idle worker) fetches to the boss; boss triage via `ho_delegate` or `ho_report(blocked)` for unclear issues (asks in chat). Acceptance: visible pipeline from issue to delegated task.
- [ ] **P6.3 Connector settings UI.** Enable per project, interval, filters, dry-run.

### Phase 7 — Provider agnosticism (Complexity: Medium/High)

- [ ] **P7.1 `ProviderRegistry` + `ModelCatalog`** in protocol/core: providers declare models, effort levels, auth kinds; agent editor shows only valid combos.
- [ ] **P7.2 `@ho/runtime-acp`.** Generic ACP client (`@agentclientprotocol/sdk`) over the runner relay: `initialize`, `session/new`, `session/prompt`, `session/update` → `RuntimeEvent`, `session/request_permission` policy. First targets: OpenCode (`opencode acp`, local models via Ollama/LM Studio), Gemini CLI (`gemini --acp`). Codex via `codex-acp` or `codex exec --json` (ChatGPT auth is possible but OpenAI discourages it for automation; API key path documented).
- [ ] **P7.3 Image variants.** Per-runtime image layers (claude-code | opencode | gemini | codex) selected by provider; still one base.
- [ ] **P7.4 Anthropic API-key mode.** `ANTHROPIC_API_KEY` from `SecretStore` as an alternative auth profile for Claude Code sessions; optional `--max-budget-usd`.

### Phase 8 — Hardening, observability, tests (Complexity: Medium)

- [ ] **P8.1 Egress allowlist proxy** container (Bun CONNECT proxy) + `HTTPS_PROXY` in sessions; per-project extra hosts.
- [ ] **P8.2 Remote/server mode.** `--host`, TLS via `Bun.serve({ tls })`, long-lived tokens, docs for SSH tunnel/Tailscale.
- [ ] **P8.3 Terminal inspector.** `@xterm/xterm` view of a session's container logs / `Bun.Terminal` exec for debugging.
- [ ] **P8.4 Tests.** `bun test`: core state machine, scheduler, sim determinism, stream-json codec fixtures, store; integration behind `HO_TEST_DOCKER=1`; UI smoke via `Bun.WebView`.
- [ ] **P8.5 Performance and disk audit.** One week of simulated use in a loop: assert no growth in images/volumes/db beyond policy.
- [ ] **P8.6 Docs and ADRs.** `docs/adr/` for D1–D10 and later decisions; codemaps.

### Future (tracked, not scheduled)

GCP `SandboxProvider` (Cloud Run Jobs), Jira/Linear connectors, Windows/Linux builds, code signing when an Apple account exists, multiple offices/companies, voice notifications.

## 5. Asset request list (for the owner to generate, LimeZu Modern Office style, 16×16, PNG, transparent)

1. **Characters** (per appearance variant, ≥4 variants + boss + postman): idle and 4-direction walk (4 frames each), sit-typing (2 frames), sit-idle, drink coffee (2), sleep (2), hand-over (2), receive (2). 32×32 canvas per frame, feet at the bottom centre.
2. **Emotion bubbles**: focused, happy, frustrated, confused/question, sleepy (zzz), relaxed, talking (…), envelope. 16×16.
3. **Tiles**: floor (office carpet, kitchen tile, lobby), walls (N/S/E/W + corners), doors, elevator (closed/open, 2 frames), windows.
4. **Furniture**: desk + monitor (on/off/typing frames), chairs (4 dirs), boss desk, reception counter, whiteboard/board, mailbox (empty/full), coffee machine (idle/brewing), fridge, sink, sofa, armchair, plant, water cooler, toilet, sink, ashtray/smoking sign, bookshelf, printer.
5. **Props**: folder/document for handoffs, coffee cup, envelope.

Naming: `assets/src/<category>/<name>[_<variant>][_<dir>][_f<frame>].png`; the packer produces `assets/dist/<sheet>.png` + `.json`.

## 6. Risks

| Risk                                                                             | Likelihood | Impact | Mitigation                                                                                    |
| -------------------------------------------------------------------------------- | ---------- | ------ | --------------------------------------------------------------------------------------------- |
| Electrobun maturity (single maintainer, docs drift, Bun-runtime mode edge cases) | Medium     | High   | Spike S2 gates D1; daemon is shell-agnostic; Tauri fallback documented.                       |
| Subscription rate limits (5-hour + weekly windows) throttle a multi-agent office | High       | High   | Concurrency caps, Sonnet/Haiku for workers, RTK, lean context, rate-limit pause, usage panel. |
| Anthropic policy changes around headless/subscription use                        | Low–Medium | High   | Use only the official CLI and `setup-token`; API-key mode as fallback (Phase 7.4).            |
| TypeScript 7.0 tooling gaps (no compiler API; `tsc -b` behaviour)                | Medium     | Low    | Oxc toolchain only; verify in P0.1; TS 7.1 restores an API.                                   |
| Docker Desktop VM RAM (~8 GB) limits concurrent containers                       | Medium     | Medium | Memory limits per session, default concurrency 2, idle stop, doctor warnings.                 |
| RTK lossy compression hides a relevant error                                     | Low        | Medium | Exclude risky commands via `~/.config/rtk/config.toml`; agents can rerun without `rtk`.       |
| Prompt cache misses across resumed sessions inflate usage                        | Medium     | Medium | Keep sessions alive within 1 h, never mutate system prompt/tool set mid-session.              |
| Sprite production bottleneck                                                     | Medium     | Low    | Placeholder generated sprites in Phase 4; asset list published early (§5).                    |
| Disk growth from images/volumes/caches                                           | Medium     | Medium | Labels + GC job + retention policies + Resources panel; P8.5 audit.                           |

## 7. Open questions for the owner

1. License for the repo (proposal: MIT).
2. Default boss model: `opus` (Team default) vs `fable` at `high` (Fable usage counts more against the window).
3. Default global concurrency (proposal: 2) and task-volume retention (proposal: 24 h).
4. Whether `.claude/` settings inside cloned project repos may load into sessions (proposal: off by default, per-project opt-in).

## 8. Log

Append entries as `- YYYY-MM-DD — <task id or topic> — <outcome> — <follow-ups>`.

- 2026-09-06 — Planning — Fresh start (previous session ignored by owner request). Researched and verified stack for September 2026 (see STACK.md sources). Owner decisions D1–D4 recorded. Plan awaiting confirmation; no code written.
- 2026-09-06 — Plan confirmed — Owner answered the open questions (D11–D14) and said "yes". Phase 0 started.
- 2026-09-06 — P0.1, P0.2 — Monorepo scaffold green: Bun workspaces + catalog, isolated linker, TS 7.0.2 (`scripts/typecheck.ts` runs `tsc -p` per workspace concurrently; `tsc -b` not used because referenced projects may not `noEmit`), oxlint 1.81 type-aware via oxlint-tsgolint (categories correctness/suspicious/pedantic/perf = error, style off with a curated allowlist; `eslint/no-redeclare` off to allow the Zod schema+type same-name idiom), oxfmt, knip, CI workflow, git hook, `@ho/protocol` with branded UUIDv7 ids. — Follow-ups: pin GitHub Actions to the majors verified today (checkout v7, setup-bun v2).
- 2026-09-06 — P0.5 (S3) — PASS. Docker Engine API 1.55 driven from Bun `fetch({ unix })` with zero dependencies: network (ICC off), volume, hardened container (`CapDrop ALL`, `no-new-privileges`, read-only rootfs + tmpfs, memory/cpu/pids limits, `Init`), start, logs (multiplexed stream demuxed), remove by label — no leftovers. Runner connect-back works: compiled `bun-linux-arm64-musl` binary dials the gateway, relays `cat` stdin/stdout, exits cleanly; create→echo→exit in 190–310 ms. **Findings:** the compiled Bun binary needs `libstdc++`/`libgcc` (plain `alpine:3.22` fails; the agent image already has them); a gateway bound to **127.0.0.1** is reachable from containers via `host.docker.internal` on Docker Desktop, so the daemon can stay loopback-only; running the runner as the container's PID 1 (with `Init`) is simpler than `exec` and auto-cleans on exit. — Follow-up: bake `ho-runner` into the image (Phase 2), keep `exec` for diagnostics only.
- 2026-09-06 — P0.6 (S4) — PASS. oRPC 1.15 contract-first (`oc`, `eventIterator`, `implement`) over WebSocket: server adapter is `@orpc/server/bun-ws` (`RPCHandler.message/close`), client is `@orpc/client/websocket` `RPCLink({ websocket })`; 1,000 streamed events in 16 ms (~63k ev/s), heap +1.9 MiB, bearer token checked at upgrade. — Follow-up: the docs summary on orpc.dev described an older link API; trust the installed `.d.mts`.
- 2026-09-06 — P0.7 (S5) — PASS. `images/git-bridge` (Alpine + git, non-root, `safe.directory=*`): clone from read-only host mount into a task volume (559 ms), agent container commits with no host mounts (255 ms), bridge pushes `ho/<task>` to the host repo via a file remote (289 ms); host working tree untouched. Volume ownership works because the image's `/work` is owned by uid 1000 (Docker copies ownership into a fresh named volume). — Follow-up: on Linux hosts the bridge must run with the host uid; Docker Desktop maps ownership transparently.
- 2026-09-06 — P0.3 image — `ho/agent:spike` built from `images/agent/Dockerfile`: Alpine 3.22 + Claude Code 2.1.236 (stable apk channel; host has 2.1.261), uid 1000, 479 MB (the `claude` binary alone is 325 MB, so the "< 400 MB" target in P2.2 is unrealistic; revise to < 600 MB). Headless auth run pending the owner's `claude setup-token` in Keychain (`security add-generic-password -s home-office -a anthropic-oauth-token`).
- 2026-09-06 — Lint calibration — pedantic rules that fought idiomatic TypeScript were switched off with reasons: `prefer-readonly-parameter-types`, `unicorn/no-useless-undefined`, `eslint/require-await` (replaced by type-aware `typescript/require-await`), `max-lines-per-function` raised to 120. `no-floating-promises` correctly caught Bun `FileSink.write/flush/end` returning `number | Promise<number>`.
- 2026-09-06 — P0.4 (S2) — PASS, D1 confirmed. Electrobun 2.0.1 is driven by Hutch: non-interactive scaffold is `bunx electrobun init <name> --template=hello-world --skip-install`; `hutch.config.ts` takes `packageManager: "bun"` (our workspace install is used) and `electrobun.version`; `electrobun.config.ts` uses `build.mainProcess: "bun"` + `build.bun.entrypoint`. The SDK is projected as TypeScript sources into `.hutch/devkit` (git-ignored); the consuming tsconfig maps `electrobun`, `electrobun/main`, `electrobun/view` via `paths` and needs `noImplicitReturns: false` because the devkit sources violate it. Dev run: main process on **Bun 1.4.0** (pinned by the devkit, not the host's 1.4.2), `bun:sqlite`, `fetch({ unix })` to Docker and a loopback `Bun.serve` WebSocket all work; typed RPC works both ways. WKWebView renders PixiJS 8 over **WebGL** (no WebGPU exposed), 60 sprites at 100–111 fps uncapped, webview→Bun WebSocket round trip 9–10 ms. Release `build --env=stable` with `mac.codesign: false` produced a 21 MB DMG, a 20 MB `.app.tar.zst` and `update.json` in ~15 s; the unsigned app launches and runs the main process. **Findings:** the native launcher keeps running after the Bun main process calls `process.exit()` in release mode, so quitting must go through Electrobun's API/window lifecycle (verify in P5.1); a missing `icon.iconset` is only a warning; `views://` assets are bundled by Hutch, so the Phase 4 plan to prebuild the UI with the Bun bundler and `copy` it in stays valid.
- 2026-09-06 — P0.8 — DONE as a manifest, not an atlas: `assets/README.md` defines `assets/src/<category>/<sprite>/<animation>[_<dir>]_f<n>.png`, `bun run assets:manifest` writes `assets/dist/manifest.json`; PixiJS multi-texture batching makes atlas packing a later optimisation. Owner asset request list is in § 5.
- 2026-09-06 — P0.3 (S1) — Script ready (`spikes/s1-claude-alpine`): reads the token from Keychain into the Docker API body only, runs `claude -p` with `--output-format stream-json`, then `--resume` against a config volume, then `--bare` (expected auth failure). **Blocked** on the owner storing the `claude setup-token` output in Keychain.
- 2026-09-06 — Phase 0 wrap-up — `bun run check` green locally and in CI (run 34000152801, 12 s). Spikes S2–S5 promoted knowledge into the Log; S1 script is ready and waits only for the owner's token. Spike S2 stays in `spikes/s2-electrobun` as a standalone Hutch project outside the Bun workspace (its devkit is a local download), excluded from lint/knip; `scripts/typecheck.ts` skips Hutch projects whose devkit is absent. Phase 1 starts with P1.1.
- 2026-09-06 — P1.1–P1.6 — DONE and smoke-tested end to end (daemon + CLI in a scratch `HO_HOME`: projects, agents with boss uniqueness, tasks create/assign/transition with typed `INVALID_TRANSITION`, chat opening an inbox task, `events.subscribe` replay + live, daemon restart rebuilding the read model from the log, graceful shutdown removing `daemon.json`). Shape: `@ho/protocol` (Zod 4 schemas, branded UUIDv7 ids, coarse persisted `DomainEvent` union built by an `event(type, payload)` helper, contract-first oRPC with shared typed errors, `eventIterator(StoredEvent)`); `@ho/core` pure (ports incl. structural `Cancellation`, `Result`, `DomainError` codes, task state machine, `ReadModel` + `applyEvent` reducer, commands returning `{ events, value }`); `@ho/store` (Drizzle 0.45 on `bun:sqlite`, WAL, migrations generated by drizzle-kit, in-process live subscription channel); `@ho/secrets` (Keychain via `security`, 0600 file fallback); `@ho/daemon` (`Office.execute` = command → append → apply, `Bun.serve` WS with bearer header or `ho.bearer.<token>` subprotocol, `daemon.json` 0600 for local clients); `apps/cli` (`ho daemon|health|project|agent|task|chat|tail`). **Findings:** Bun 1.4.2 throws on `server.upgrade(req, { headers: {} })` — pass a `Headers` instance or no options; `erasableSyntaxOnly` forbids constructor parameter properties; Zod 4 nested defaults need `.prefault({})`; tsgolint's `no-floating-promises` correctly flags Bun `FileSink.write/flush/end` (`number | Promise<number>`); `Promise.withResolvers` keeps async channels closure-free. Deferred: session/handoff events (Phase 2/3), CLI binary build script exists (`bun run build` in `apps/cli`).
- 2026-09-06 — P2.1, P2.3, P2.4 — DONE; P2.2, P2.5, P2.6 partially (see below). The daemon now runs sessions end to end: scheduler (pure `planSessionStarts`) → `session.started` → git-bridge clones the host repo into a labelled task volume on branch `ho/<slug>-<id>` → hardened sandbox with `ho-runner` as PID 1 → runner dials `/runner` with a one-time token → `claude -p --input-format stream-json …` spawned with the OAuth token only in its environment → normalised events fan out live (`ho session watch`) → usage/artifacts/status persisted → sandbox removed. Verified with a **dummy token**: Claude Code 2.1.261 in the sandbox answered `401 OAuth access token is invalid`, surfaced as `authentication_failed` + an error result, task → `blocked` with the message as report, session → `failed`, no leftover containers; the whole cycle took ~10 s. The real-token run (P0.3/S1) is the only unverified step and needs the owner's `claude setup-token` in Keychain.
- 2026-09-06 — Findings — `--permission-prompts` needs Claude Code ≥ 2.1.259 (the `stable` apk channel shipped 2.1.236), so the agent image now installs from the `latest` channel and the adapter no longer passes the flag (redundant under `bypassPermissions`). Volumes referenced only in `Mounts` are auto-created **without labels**; every volume is now created explicitly with `ho.kind=task-volume|claude-config`. `--settings` and `--mcp-config` accept inline JSON, so the sandbox needs no config files. Secret store kind is configurable (`secrets.store: file`) for servers and tests; `ho secret set` reads the value from stdin or a hidden prompt, never argv.
- 2026-09-06 — Still open in Phase 2 — P2.2: RTK hook and role plugins (`@ho/agent-kit`) are not in the image yet; P2.5: `gh pr create` option and git-URL projects; P2.6: `ho usage`/`ho resources` summaries (data already flows through `session.usage_recorded` and `system.doctor`). GC job runs at start and every 30 min (`ho gc`); task volumes are kept for `retention.taskVolumeHours` (24 h).
- 2026-09-06 — P0.3 (S1) — DONE. The owner ran the documented flow with a real `claude setup-token` in Keychain: a sandboxed session produced the `ho/*` branch in the host repository. Later the same day two haiku sessions and one more on a git-URL project completed in 20–60 s each (6–8 turns, ~50–60 input, ~900–1,100 output, ~106–128k cache-read tokens per session).
- 2026-09-06 — P2.2 — DONE. `@ho/agent-kit` vendors three role packs from ECC `e04ea0b` (MIT; worker: coding-standards, verification-loop, git-workflow, error-handling, bun-runtime; reviewer: coding-standards, security-review, plankton-code-quality, verification-loop; boss: agentic-engineering, context-budget, search-first), synced into the image context and loaded per `agent.skillPack` via `--plugin-dir /opt/ho/plugins/<pack>`; the `init` runtime event confirmed `plugins=[ho-worker]` in live sessions. RTK 0.48.0 is compiled from source in a `rust:1-alpine` stage (no aarch64-musl release exists; ~6 min once, cached afterwards) and its hook is installed with `rtk init -g --hook-only --auto-patch` into the image's `~/.claude/settings.json`, which Claude Code reads (`--setting-sources user`) and RTK checks — placing the hook only in inline `--settings` made RTK print "No hook installed" into every tool result. Image: 480 MB, Claude Code 2.1.263.
- 2026-09-06 — P2.5 — DONE. git-URL projects are mirrored on the host (`$HO_HOME/mirrors/<projectId>.git`, `git clone --mirror`, `fetch --prune` before each session); bridges see only the mirror; after the bridge push the daemon forwards the task branch to the remote with the owner's credentials. **Finding:** a mirror remote rejects refspecs (`--mirror can't be combined with refspecs`), so the push runs with `-c remote.origin.mirror=false` and an explicit refspec — verified end to end with a `file://` bare remote. Pull requests are a per-project policy (`publish.mode`, `draft`) executed by the host's `gh pr create`; delivery failures never fail the task (branch is the deliverable). CLI: `ho project add|set --pr on|off --draft on|off`.
- 2026-09-06 — P2.6 — DONE. `usage.summary` (totals, by agent/project/day, rate-limit incidents from stored events) → `ho usage [--since 24h|7d]`; `resources.inventory` (containers and volumes with kind, session, age, size + snapshot) → `ho resources`; GC via `ho gc`. Cleanup verified: no containers left after sessions; task/config volumes expire after 24 h.
- 2026-09-06 — Assets — Documented the generator delivery format (`@8x` PNG, horizontal `_strip<N>`, `autotile3x3`, no `w` direction) in `assets/README.md`; added P4.0 (importer) to Phase 4. Owner received a Czech generation prompt matching these conventions.
- 2026-09-06 — P3.1–P3.6 — DONE and verified end to end with the owner's token (all roles on haiku/low): a chat message with no project became a triage task in the auto-created **Office** project (`repo.kind = "none"`, floor `lobby`); the boss session found the MCP tools through Claude Code's tool search (`mcp=[ho]`, pack `ho-boss`), called `ho_delegate` (task assigned to Pam with acceptance criteria), then `ho_report(done)`; its final text was posted to chat. Pam's work session committed and called `ho_report(review)`, which auto-assigned the project's reviewer; Dwight's review session diffed the branch and filed `ho_review(request_changes)` with file:line findings; Pam's second session **resumed the same Claude conversation** (`--resume`, identical runtime session id, no re-reading) and fixed the file in one edit; Dwight's second review resumed too and approved. Timeline 09:48:43 → 09:50:27 (1 min 45 s, 5 sessions, 26 turns, 222 input / 7,969 output / 501k cache-read / 55k cache-write tokens, 0 rate-limit incidents, 0 daemon warnings). Branch `ho/add-code-of-conduct-md-…` landed in the host repository; no containers left behind.
- 2026-09-06 — Phase 3 design notes — HO MCP server: stateless `WebStandardStreamableHTTPServerTransport` on the daemon's `/mcp`, one `McpServer` per request bound to the caller's session (tools depend on mode: work gets `ho_handoff`, review gets `ho_review`, triage gets `ho_delegate`/`ho_reply`; all get `ho_report`, `ho_ask_human`, `ho_task_status`, `ho_list_agents`, `ho_list_projects`). Tasks gained `kind`, `reviewerId`, `reviewRounds` and `notes` (handoff/review/question/answer/report); the opening message of a resumed session lists the notes since the agent's previous session. Per-agent Claude config volumes (`ho-task-<id>-claude-<agent>`) carry conversations across sessions; task volumes carry the checkout, so review sessions see the author's branch without a new clone. Wall-time budget aborts sessions; review rounds are capped by the author's `maxReviewRounds`. `handoff.requested` is emitted for the office animation but sessions do not yet wait for it (P4.6). Settlement rules: work without `ho_report` → report filed from the result text; review without a verdict → task blocked for the human; triage without `ho_reply` → final text posted to chat.
- 2026-09-06 — P4.0 — DONE. Pure-Bun PNG codec (`scripts/lib/png.ts` + `png-decode.ts`: colour types 0/2/3/4/6, 8/16-bit, filters 0–4; encoder RGBA8 with `Bun.deflateSync` wrapped in a zlib header — Bun's deflate/inflate are **raw** DEFLATE, so the zlib header and Adler-32 are handled by hand). `bun run assets:import` reads `assets/inbox/<category>/<sprite>/<name>`, samples each 8×8 block centre of `@8x` files, keys out `#FF00FF`, slices `_strip<N>` and `autotile3x3`, validates sizes and writes native frames to `assets/src`; `bun run assets:manifest` builds `assets/dist/manifest.json` (`<category>/<sprite>` → animation → frames). Verified with a synthetic 1024×256 magenta strip → four 32×32 frames with proper alpha. `bun run assets:placeholders` generates the complete placeholder set from §5 (40 sprites, 225 frames) so the office renders before the owner's sprites arrive; real imports overwrite the same file names.
- 2026-09-06 — P4.1/P4.2 — DONE (chat pairs deferred to P4.6 polish). `@ho/sim` is pure (no Bun/DOM types): mulberry32 RNG, 4-connected A* grid, procedural floor templates (Lobby: reception, mailbox, boss office, kitchen, toilets, relax room, smoking room, elevator; project floor: up to 8 desks, kitchenette, toilet, relax corner), world with actors, anchor reservations, plan steps (`walk` with lazy A* around standing actors, `elevator` = 1.5 s hidden transfer between floors, `dwell`, `hold`, `emit`), needs (coffee/restroom/smoke/relax, thresholds + utility pick), intents (`assignWork` to a free desk or the boss desk, `releaseWork` with celebration, `handoff` walks to the recipient across floors and emits `handoff_delivered`, `sleep`/`wake`, `setEmotion`) and `emotionFor(RuntimeEvent)`. Headless smoke run: every anchor on both floors reachable from the elevator; boss → boss desk, worker → project desk via elevator, handoff delivered in 12 s of sim time, idle needs and reservations cycle over 2 simulated minutes without deadlock. The 10-agent/10-minute soak moves to the UI debug harness (P4.4).
- 2026-09-06 — P4.3–P4.5 — DONE. `@ho/ui`: React 19 built with the Bun bundler (`reactCompiler: true`, Tailwind 4 via `bun-plugin-tailwind`, `bun run ui:build` → `packages/ui/dist`, 3 files / 966 KiB), Zustand store, oRPC WebSocket client with the token in a `ho.bearer.<token>` subprotocol. `ho ui` opens `http://127.0.0.1:47800/#token=…` — the fragment never reaches the server; the page moves it to sessionStorage and wipes the address bar. The daemon serves the bundle at `/` (SPA fallback) and sprites at `/assets/` from `config.ui` (defaults to the repository layout), with a traversal guard. State: the read model is rebuilt client-side from `events.subscribe` (new `events.head` marks the end of the replay) and exposed to React as immutable snapshots — **finding:** the React Compiler memoises JSX derived from a mutated module-level model, so components must read reactive snapshots, never the mutable model. PixiJS 8 renderer: baked tile layer per floor (`cacheAsTexture`), 3×3 wall autotile by neighbours, z-sorted furniture and characters, emotion bubbles, name labels, click-to-inspect, integer pixel scale, 30 fps cap, paused when hidden. Panels: Chat (boss triage / project inbox / answers to agent questions), Board (per-project columns), Agent inspector (sessions, live tool log, branch, usage), Usage, Resources (prune), Settings (credentials via Keychain, projects, agents with sprite/gender picker and project membership). Verified in the in-app browser against the Phase 3 data.
- 2026-09-06 — P4.6 — DONE and verified with real sessions (haiku). Delegation (`ho_delegate`), review requests and change requests now emit `handoff.requested` plus a `handoff` note. The daemon's `HandoffGate` holds the recipient's session while at least one office UI keeps the `office.presence` stream open and the task's latest handoff note is fresh (20 s) and undelivered; the UI reports `office.handoffDelivered` when the handover animation finishes. Log of the third run: `handoff.requested` 12:47:24 → boss walks from his desk to Pam and hands over → "handoff delivered by the office" 12:47:42 → Pam's session scheduled 12:47:43, no timeout. **Findings:** (1) the session end of the boss used to replace the actor's steps and cancel the walk — intents now preserve an in-progress handoff (`pendingHandoff`); (2) a hidden window gets no animation frames, so the bridge reports pending handoffs immediately when the document is hidden (the 20 s timeout stays as the last resort); (3) the in-app browser pane counts as hidden, so the sim was driven manually from the console during verification. Three chat requests (SECURITY.md, CONTRIBUTING.md, CHANGELOG.md) ran through triage → work → review with zero daemon errors and no containers left behind.
- 2026-09-06 — Phase 4 follow-up — Owner's first local run had no boss agent: `triageMessage` stores the chat message but creates no task, so nothing answered. The chat panel now shows a hint while the office has no boss and renders rejections as a visible red block. `fileReport` no longer parks work tasks in `review` when the project has no reviewer (status `done`, reason "no reviewer in this project"); the branch stays the deliverable for the human. Still open for the first-run wizard (P5.2): create the boss and a default team on first launch.
- 2026-09-06 — D15 — Browser tooling in the agent image: Alpine 3.22 arm64 ships `chromium` 142.0.7444.59 (community) and `nodejs` 22.23.2 (main); Playwright officially skips musl only because of its downloaded browsers, so `@playwright/mcp` runs on the system Node with `--executable-path /usr/lib/chromium/chromium`. Both MCP servers are installed from `images/agent/mcp/package.json` into `/opt/ho/mcp` at build time (`PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD`, `PUPPETEER_SKIP_DOWNLOAD`), Bun comes from the official `bun-linux-aarch64-musl.zip` with a pinned SHA-256. `RuntimeSessionSpec.mcpServers` now carries http or stdio entries (`type` is mandatory in Claude Code's `--mcp-config`); work and review prompts explain the browser tools, the 127.0.0.1-only dev servers and the screenshot directory.
- 2026-09-06 — D15 verification — `ho image build` rebuilt `ho/agent:dev` in 1 min 54 s (RTK stage cached); the image grew from 480 MB to 1.76 GB, almost all of it the apk layer (Chromium with its Mesa/ICU/font dependencies, Node, npm). Hardened-container smoke test as uid 1000 with CapDrop ALL, no-new-privileges, read-only rootfs and a 256 MB tmpfs: Chromium 142 renders a headless screenshot, a Bun server on 127.0.0.1:8080 is reachable from Chromium, both MCP servers start (crashpad and Vulkan warnings on stderr are harmless). Real run on haiku: the boss delegated to Pam, whose work session (23 turns) created `demo/index.html` + `demo/serve.ts`, started the Bun server, drove it with the Playwright MCP tools, clicked the button and committed a 75 KB `demo/screenshot.png` showing the toggled paragraph; Dwight's review followed. Follow-ups: trim the image (drop `chromium-swiftshader`/unneeded Mesa bits, evaluate `--slim` for Chrome DevTools MCP), silence crashpad via `--chromeArg=--disable-crash-reporter`, and note that the image is rebuilt only by `ho image build`, not at session start.
- 2026-09-06 — Daemon packaging prep — `startDaemon({ home, overrides, resourcesRoot, logFile })`: `resolveResources(root)` locates image contexts, the UI bundle, sprites and Drizzle migrations under one root (repository in development; `HO_REPO_ROOT` overrides); `ensureImages` compiles ho-runner and syncs the role packs only when their sources exist, otherwise it expects the prebuilt binary and packs inside the context; `openDatabase(path, { migrationsDir })`; `createLogger(level, file)` writes NDJSON to a file for the desktop app. `ho daemon --ui` prints the office URL and where the token lives (P5.4; the daemon already served the bundle since P4.3). RPC context assembly moved to `rpc/context.ts` and daemon.json handling to `daemon-info.ts` to keep `index.ts` under the 20-dependency lint limit.
- 2026-09-06 — P5.1 — DONE (D16). `apps/desktop` (Hutch project inside the Bun workspace; `hutch electrobun sync` projects the 2.0.1 devkit in about one second from the local release cache) runs `@ho/daemon` in the Bun main process and opens the daemon-served UI in a `BrowserWindow`. Verified with `hutch electrobun dev` against a fresh `HO_HOME` on port 47811: resources resolved to `Contents/Resources/app/ho`, the WKWebView networking process held an authenticated WebSocket to the daemon (token via `preload` → sessionStorage, zero rejected upgrades), SIGTERM → `before-quit` veto → "daemon stopping" → daemon.json and desktop.lock removed → launcher exited 0 within 2 s. **Findings:** the Electrobun main bundle is produced by the Bun bundler with `bun:*` kept external, so `@ho/daemon` and its dependencies (drizzle, pino, oRPC, MCP SDK) bundle as-is; `PATHS.RESOURCES_FOLDER` is relative to the launcher's cwd (`Contents/MacOS`), which Hutch sets; `exitOnLastWindowClosed: false` plus an explicit close → shutdown → `Utils.quit()` keeps quitting deterministic; the ho-runner build is reproducible (the bundled context hashed identically to the repository's, so the packaged app does not trigger an image rebuild). Electrobun RPC was not needed: menu, dialogs and external links are native-side only, and the UI stays free of Electrobun code (browser parity).
- 2026-09-06 — P5.2 — DONE. First-run checklist in `@ho/ui` (`src/setup/`): Docker (API ≥ 1.44), agent images (streamed `system.buildImages` log with elapsed time), Claude token (paste → Keychain), default team (Michael boss opus/high, Pam and Jim workers sonnet/medium, Dwight reviewer sonnet/high, sprite sets boss/agent-a/b/c), optional first project (workers and reviewer join it) and a smoke test (hello to the boss, reply shown inline). Auto-opens on connect when anything is missing unless dismissed (localStorage); the header's Setup button reopens it. Verified in a fresh `HO_HOME`: Docker/images/token green, "Create the default team" hired four agents, the smoke test (boss switched to haiku for the check) ran one triage session (1 turn, 10 in / 86 out tokens) and posted "I'm online and ready to manage the office." to chat about 50 s after the click. Deviation: the smoke test uses the boss's configured model rather than forcing haiku, because triage is what the user will run next anyway; the copy states the expected cost. Floor templates needed no work (procedural since P4.1).
- 2026-09-06 — Bug (Phase 4) — Zod 4 `.partial()` re-applies field defaults, so every `agents.update` patch from Settings reset `basePrompt` to "", `skillPack` to "none", `projectIds` to [] and budgets to defaults (a model edit silently dropped the boss's skill pack); `projects.update` likewise reset the default branch and publish policy. `AgentPatch`/`ProjectPatch` now unwrap the defaulted fields before `.partial()`; verified `{ patch: { model } }` parses to exactly `{ model }`.
- 2026-09-06 — P5.3, P5.4 — DONE. `bun run desktop:build` (= `desktop:prepare` + `hutch electrobun build --env=stable`) took about 30 s (11 s of zstd compression) and produced `apps/desktop/artifacts/macos-arm64-HomeOffice.dmg` (50 MB), `stable-macos-arm64-HomeOffice.app.tar.zst` (49 MB) and `stable-macos-arm64-update.json`; the `.app` is 47 MB because the 74 MB ho-runner and the resources compress well, and the generated `icon.iconset` became `AppIcon.icns`. First launch of the stable app self-extracts into `~/Library/Application Support/cz.ondrejmisak.home-office/stable/` (2.4 s + 0.2 s) and installs an uninstaller there; the daemon then started from `Contents/Resources/app/ho`, served the UI, accepted the webview's WebSocket and cleaned up on SIGTERM exactly like the dev build. `release.yml` runs on `v*` tags on `macos-26`: Bun 1.4.2, Hutch 0.25.0 via the pinned install script, `hutch electrobun sync`, `bun run check`, app version stamped from the tag, artifacts + `SHA256SUMS.txt` attached with `gh release create`, notes with the one-time `xattr -cr`. `ci.yml` now installs Hutch and projects the devkit so the desktop workspace is typechecked on every push (`scripts/typecheck.ts` skips it silently otherwise). P5.4: the daemon has served the bundle since P4.3; `ho daemon --ui` now prints the URL and points at daemon.json for the token. **Not yet exercised:** an actual tag push (owner's call; the workflow follows the verified local commands). Follow-ups: Gatekeeper still prompts on an unsigned app; the Electrobun `Updater` is not wired although `release.baseUrl` points at GitHub Releases; the icon is a generated placeholder; the main process runs on Bun 1.4.0 (devkit toolchain) while the CLI uses 1.4.2.

- 2026-09-06 — Phase 4 art follow-up — Saved the owner-approved final office concept unchanged as `assets/reference/office-base-v1.png` (SHA-256 recorded in `docs/OFFICE-ART.md`). Audited the asset importer, PixiJS renderer, floor geometry, seat selection and elevator travel; documented the production sequence and corrected the asset README to match current limits. — Follow-ups: deterministic walkable layout, one playable art sample, then reusable asset batches and renderer/simulation integration; production sprites and runtime changes are not implemented by this documentation task.

- 2026-09-06 — Phase 4 art follow-up, layout lab — Implemented the approved composition as pure 80×46 floor data (6 DEV / 2 QA / 2 analyst / 1 boss), an on-demand reachability audit, and `/office-lab` with the existing Pixi character renderer and simulation executor. `bun run office:lab` serves a loopback-only live preview on port 47810, watches UI/sim/native sprite changes and reloads after rebuild; no daemon or model sessions. Verified 30/30 anchor routes, 2,729 connected walkable cells, all door thresholds, elevator arrival and a sequential visit to every destination; browser confirmed arrival/typing, grid and detail controls without console errors. `bun run check` passed. — Follow-ups: polished sprite-density sample and foreground/glass/seated artwork, then production role-aware seat selection and live-floor integration; lab objects remain geometric stand-ins.

- 2026-09-06 — Phase 4 layout corrections — Closed the two-cell gap between the reception backdrop and elevator shaft. Kitchen glazing was already blocking A*, but looked like a low railing; it now renders as tall translucent fixed panels with foreground sorting, explicit wall construction and glass collision diagnostics. Verified routes to the terrace use the kitchen side door and reception traffic goes around the right-hand entrance; 30/30 anchors and all 2,727 remaining walkable cells are connected. Browser confirmed both visual changes; `bun run check` passed. — Follow-ups: production artwork.

- 2026-09-06 — Phase 4 annotated kitchen layout — Matched the owner's red/green annotation: removed the kitchen west wall at x=54, y=21–32 and extended fixed south glazing across x=49–68 at y=33, closing the corridor-to-terrace gap against the meeting-room wall. Verified open west access, every glass cell blocked, east-side terrace detour, 30/30 reachable anchors and 2,734 connected walkable cells; confirmed the updated geometry in the live browser. `bun run check` passed. — Follow-ups: production artwork.

- 2026-09-06 — Phase 4 kitchen doorway correction — Removed the owner-marked kitchen-to-spa door, restoring the solid east wall at x=69, y=23–25. Kitchen access stays open on the west; the terrace route uses the north entrance from the main corridor. Verified 30/30 reachable anchors, all 2,731 walkable cells connected, solid east wall and a valid route via the terrace north entrance. `bun run check` passed. — Follow-ups: production artwork.

- 2026-09-06 — Phase 4 toilet alignment — Moved both WC stalls two cells north to share the restroom upper wall, removing the owner-marked gap. Shifted toilets, interaction anchors and doors together, and kept the room label clear of the fixtures. Verified 30/30 reachable anchors, all 2,740 walkable cells connected, routes from the restroom entrance to both stalls and sinks, and the visual result in Office Lab. `bun run check` passed. — Follow-ups: production artwork.

- 2026-09-06 — Phase 4 room surface boundaries — Included the owner-marked west strip in the kitchen/dining floor and room bounds; clipped reception flooring and its label to the area below its backdrop, excluding elevator approaches and the right corridor. Unified floor painting for enclosed and open room metadata. Verified walls, doors and interaction anchors are identical to the preceding layout; all 30 anchors and 2,740 walkable cells remain connected. Confirmed both surface corrections in the live browser; `bun run check` passed. — Follow-ups: production artwork.

- 2026-09-06 — Phase 4 route polish — Added cached static clearance and direction-aware A* turn costs to prefer corridor centres and long straight segments. The terrace-to-boss sample dropped from 22 turns / 123 steps to 5 turns / 119 steps compared with clearance alone. Verified all 900 anchor pairs, actual simulated visits to all 30 destinations, single-cell passage, dynamic blocking and clearance cache invalidation; live browser shows straight route segments. `bun run check` passed. — Follow-ups: production artwork.

- 2026-09-06 — Phase 4 DEV 3 art sample — Generated and saved a versioned workstation source sheet and prompts; added reproducible chroma-key/native-frame import and four separate desk, carpet, empty-chair and occupied-chair textures. Office Lab replaces only DEV 3, offers a focused sample control, and swaps the static seated pose on arrival/departure through an optional floor actor texture override. Collision geometry is unchanged. Browser verified clean alpha, keyboard alignment, seated pose and return to empty chair on departure; `bun run check` passed. — Follow-ups: owner review of style/density, matching walking/typing animation and independently layered seated character; this calibration pose is static and existing walking sprites remain placeholders.
