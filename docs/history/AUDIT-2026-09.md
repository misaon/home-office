> Report of a previous audit (2026-09-07), retained for provenance. It describes the repository as it was
> then; the current independent audit lives in [`audit/`](../../audit/AUDIT.md). Do not read versions,
> benchmarks or guarantees here as current facts.

# Independent monorepo audit — September 2026

Audit started 2026-09-07 from `origin/main` at `f3e6eb8cda5f86658680e71c48ddce9b4fc13156`.
Work branch: `codex/monorepo-audit-2026-09`. The reviewed areas, implemented repairs and limits of verification are recorded below.
No new tests or code comments are introduced. Original sprite sources are preserved.

## Verified baseline

- Approximately 19,475 lines of TypeScript, 25,912 lines including configuration and documentation.
- Bun 1.4.2, Docker Engine 29.7.2; initial full `bun run check` passed all 16 compiler targets,
  type-aware lint, formatting and Knip. Knip reported a CSS coverage configuration hint.
- The npm registry was queried for every root catalog entry and sandbox CLI/MCP dependency.
  Most pins match current releases; Oxc offers newer formatter and linter releases.
- Passing static checks did not establish runtime lifecycle, access-control or data-integrity correctness.

## Findings and implemented repairs

| Area             | Finding                                                                                     | Remediation                                                                                      |
| ---------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| Event sourcing   | Concurrent commands decide against the same pre-append projection                           | Serialize decision, append and projection application; failed commands do not poison the queue   |
| Event replay     | Repeated events could double-count usage and chat                                           | Ignore already-applied sequence numbers                                                          |
| Storage          | Any Zod replay error silently archives the database and starts empty                        | Fail with the database preserved; require an explicit migration or restore                       |
| Subscriptions    | Unbounded queues, stale subscribers, abort-listener leaks                                   | Bounded queues, constant-time dequeue, disposal on cancellation, overflow and consumer return    |
| Runner protocol  | Malformed JSON escapes parsing; hello/spawn/disconnect can hang                             | Validate state and messages; bound connection/spawn time; reject and close on failure            |
| Runner output    | Exit can overtake stdout; unlimited line and socket buffering                               | Drain both streams before exit; bound output lines and queued bytes                              |
| Session shutdown | Database closes while sessions still run                                                    | Track pending starts and running promises; abort and await cleanup                               |
| Credentials      | Secrets temporarily created with default permissions; concurrent writes lose updates        | Atomic owner-only replacement and serialized mutations                                           |
| Keychain         | Shell subprocess exposes credential in argv                                                 | Native asynchronous `Bun.secrets` API, retaining service/account names                           |
| Static serving   | Malformed percent encoding throws; symlinks escape root; dashed sprite names cached forever | Validate paths, resolve symlinks, revalidate mutable files, add response security headers        |
| Network          | Remote plaintext bind allowed despite documentation promising TLS                           | Validate loopback-only binding until a real remote TLS mode exists; reject cross-origin requests |
| Docker           | Requests can hang; logs and swap unbounded                                                  | Request deadlines, bounded local logs, memory-plus-swap limit                                    |
| Publishing       | Force pushes can overwrite later work                                                       | Normal pushes surface conflicts without overwriting branches                                     |
| MCP              | A session can read another floor's task by ID                                               | Enforce project ownership on task status                                                         |
| Domain           | Removing reviewers/floors can orphan live sessions; task parents can cross floors           | Validate session/task ownership and deletion preconditions                                       |
| Project identity | Changing repository reuses task volumes from a different repository                         | Repository identity is immutable; create a new floor                                             |
| Chat             | Schema accepts both destination fields or whitespace-only messages                          | Exactly one destination and nonempty trimmed text                                                |

## Research sources

Simulation and rendering: weighted A* now uses the dependency-free TinyQueue heap; temporary and removed
actor reservations are released, queued mail is retained, and reachability auditing uses one flood fill.
The simulation advances in bounded fixed steps. Hidden windows stop ticking, only visible actors update,
floor views use a two-entry cache, and resize work runs on resize rather than each frame. Live logs retain
at most 20 sessions with 300 events each. Sprite loading is coalesced and unmount races are guarded.

Sprite tooling: Sharp 0.35.4 replaces the handwritten PNG encoder/decoder. Input crops, strip lengths,
keys and dimensions are validated. Original sprites are preserved. All 16 compiler targets, lint,
formatting, Knip, the 250-frame manifest and the production UI build passed after this batch.
The manifest correctly reports 47 missing furniture keys rendered as stand-ins; these are missing art,
not missing source files that this audit should fabricate.

Sources were accessed during this audit; version claims are observations on the access date, not permanent guarantees.

- [Bun Secrets](https://bun.com/docs/runtime/secrets): experimental native credential storage, used to remove the shell adapter.
- [Bun install](https://bun.com/docs/pm/cli/install) and [isolated installs](https://bun.com/docs/pm/isolated-installs).
- [PixiJS performance guidance](https://pixijs.com/8.x/guides/concepts/performance-tips).
- [Phaser 4 releases](https://phaser.io/download/phaser4): Phaser 4.2.1 is available; a migration still needs an application-specific reason.
- [Docker security](https://docs.docker.com/engine/security/).
- [Codex configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference), fetched through the official documentation connector.
- [Codex ACP adapter](https://github.com/agentclientprotocol/codex-acp/blob/main/README.md).
- [Claude Code model configuration](https://code.claude.com/docs/en/model-config).
- [Oxc releases](https://github.com/oxc-project/oxc/releases).
- [Sharp raw image API](https://sharp.pixelplumbing.com/api-constructor/).
- [TinyQueue](https://github.com/mourner/tinyqueue).

## Lifecycle, delivery and CLI batch

Daemon startup and shutdown now use `AsyncDisposableStack` with an atomic, refreshed proper-lockfile
lock. Failed startup releases acquired resources; repeated stop calls await the same cleanup. Scheduled
work, intake, garbage collection and delayed boss messages are awaited before closing storage. Restart
reconciles interrupted sessions and their managed containers, leaving affected tasks blocked for explicit
resumption. A manual run using a temporary state directory and an unavailable Docker socket verified
HTTP health, duplicate-instance rejection, simultaneous stop calls and successful restart without touching
existing office state or containers.

Work reports are finalized after publication succeeds. Delivery failures now propagate and block the
task; an existing open PR is reused. Git bridges disable hooks/fsmonitor and clone without hardlinks;
new branches use immutable task IDs, existing recorded branches remain valid. Refreshing a remote
repository no longer prunes or overwrites unpublished task branches. GitHub URL matching is anchored,
remote inputs require HTTPS/SSH without embedded passwords, and GitHub polling paginates all open
issues instead of repeatedly seeing only the newest 50. Cancellation listeners and subprocess deadlines
are bounded. CLI task assignees resolve within their floor, ambiguous session suffixes fail, relative
paths resolve on the client, mutually exclusive options are rejected, and Docker/image failures give
`ho doctor` a nonzero exit code. IPv6 daemon URLs are bracketed.

Oxc upgraded to oxlint 1.82.0 / oxfmt 0.67.0. The esbuild override to 0.28.2 removes the vulnerable
Drizzle Kit transitive version; `bun audit` reports no vulnerabilities across 365 packages. This override
requires continued compatibility checks when Drizzle Kit changes. Full compiler/lint/format/Knip checks
pass. No new tests were written.

- [proper-lockfile design and API](https://github.com/moxystudio/node-proper-lockfile).
- [GitHub pagination](https://cli.github.com/manual/gh_api).
- [esbuild advisory](https://github.com/advisories/GHSA-67mh-4wv8-2f99).

## UI, build and CI batch

TanStack Query owns abortable, deduplicated health/resource/usage requests with bounded caching and
foreground-only polling. Setup no longer issues duplicate doctor requests; editing a repository source
immediately invalidates the previous inspection. Chat only clears a draft after a successful send,
blocks duplicate sends, respects IME composition and remounts floor-specific state on navigation.
React hook and accessibility rules are enabled. CSS participates in Knip through its owning workspace;
unused configuration exceptions were removed. Bun's text lockfile is visible in diffs.

The UI build publishes a complete staging directory and retains the last successful bundle on failure.
Watch rebuilds serialize; sprite manifest revisions derive from content. Typecheck limits parallel
compiler processes to four and fails if the desktop devkit is missing. `bun run devkit` installs a
revision- and checksum-pinned Hutch archive locally, avoiding execution of a changing remote installer.
The root dependency audit and Drizzle generation compatibility check passed (no schema changes).

CI pins actions to verified commit SHAs, uses read-only checkout credentials, checks compiled CLI/runner
and UI builds, and runs dependency audits. Releases validate version tags and main ancestry, inject the
version through configuration, and isolate publication privileges in a separate job. Dependabot tracks
Bun, Actions and Docker weekly. The release workflow remains unsigned; no release was published by the
audit. The local macOS toolchain setup and all 16 static targets passed.

- [React Effect Events](https://react.dev/reference/react/useEffectEvent).
- [TanStack Query defaults](https://tanstack.com/query/latest/docs/framework/react/guides/important-defaults).
- [Knip compilers](https://knip.dev/features/compilers).
- [Hutch 0.25.0 release manifest](https://hutch.blackboard.sh/hutch/releases/0.25.0/manifest.json).
- [GitHub Actions security](https://docs.github.com/en/actions/reference/security/secure-use).
- [Dependabot ecosystem support](https://docs.github.com/en/code-security/reference/supply-chain-security/supported-ecosystems-and-repositories).

## Sandbox and provider batch

All four provider images and git-bridge built successfully under separate `ho/audit-*` tags. Existing
application images and containers were preserved. Alpine 3.24.1 replaces 3.22 so Chromium's community
packages are on a supported branch; base images are pinned by manifest digest. RTK is pinned by source
commit, Bun by archive digest, Claude Code by APK version (2.1.263-r1), and all npm provider/browser trees
now use committed npm lockfiles and `npm ci`. Each of those four trees passed `npm audit --omit=dev`.
Docker contexts exclude unrelated files. Unsupported image architectures fail explicitly: agent runner
and Bun bundles currently target Linux arm64. Generated desktop icon PNGs are rebuilt from their source
script and no longer tracked; original sprite assets are unchanged.

Image freshness hashes now cover current runner/protocol sources, Bun lockfile, plugin metadata including
hidden files, and provider lockfiles. Previously an old copied runner/plugin directory could be reported
as current. Packaged applications hash their actual bundled runner and plugins.

ACP startup requests time out after 30 seconds. Shutdown escalates through TERM/KILL with a final
15-second deadline in both runtime adapters. Cancellation listeners are removed; the ACP stream reads
on demand, accumulated result text and outstanding tool calls are bounded. The existing ACP mock spike
passed negotiation, authentication, permission selection, tool result, final response and process exit.
Codex model and reasoning settings now use the pinned adapter's documented `CODEX_CONFIG`; API-only
headless login is explicit. Gemini suggestions remove the shutdown `gemini-3-pro-preview`, expose current
3.8 Flash / 3.5 Flash-Lite and retain freeform model IDs. Existing configured agent models are preserved.

Playwright MCP is the default browser toolset. Chrome DevTools MCP is opt-in with `browser.devtools`,
reducing tool-schema overhead and avoiding a second browser unless needed. Both get writable temporary
XDG config/cache directories. The real Playwright MCP server successfully opened a page under non-root,
read-only, capability-dropped, no-network, memory/PID-limited Docker settings. RTK's Claude hook was
verified to rewrite `git status` to `rtk git status`; its emitted installation warning is misleading,
but the generated settings and actual hook invocation work. No percentage savings are claimed without
application measurements. CLI and browser WebSocket handshakes now have a 10-second deadline.

The complete unsigned macOS arm64 desktop build succeeded locally, producing a DMG. Builds include all
image contexts, cross-compiled runner, migrations, UI and sprites. This does not verify every paid model
or provider account: no production credentials were used for audit smoke checks.

- [Alpine support windows](https://alpinelinux.org/releases/).
- [npm ci](https://docs.npmjs.com/cli/v11/commands/npm-ci).
- [RTK source](https://github.com/rtk-ai/rtk/tree/fde0a8f185945556f51718de0f4c430bb62b3df6).
- [Pinned Codex ACP runtime options](https://github.com/agentclientprotocol/codex-acp/blob/061f9a4a2e463a220d7a3ab2ae5e9732837085ef/README.md#runtime-options).
- [Gemini model shutdown schedule](https://ai.google.dev/gemini-api/docs/deprecations).
- [Claude model IDs](https://platform.claude.com/docs/en/models/overview).

## Final integration review

The production office was exercised against a separate temporary daemon home containing two synthetic
floors, with file secrets and an unavailable Docker socket. No paid provider session or real project
mutation was needed. Browser verification covered setup/dismissal, sprite rendering, floor switching,
chat draft isolation, settings, usage and the unavailable-resource error state. Resizing from 1280 × 720
to 1024 × 768 resized the canvas without document overflow. This is a short functional check, not a
sustained memory/CPU benchmark or a complete accessibility assessment.

This check found an integration regression from the new strict CSP: Pixi's default blob-worker probe
could remain pending, and its default renderer needs dynamic function generation. Sprite loading now
uses the supported non-worker preference and the current `texture` parser ID; the official
`pixi.js/unsafe-eval` compatibility module supplies implementations that avoid eval. The CSP continues
to prohibit eval and blob scripts. Renderer initialization failures are now visible in the office.
The linter allows this one intentional side-effect import by exact module name. Credential fields have
accessible names. The final production UI bundle is about 1100 KiB uncompressed.

Building the final images through HO's own adapter also exposed a second, inconsistent image-inspection
schema. Images without labels now work in both status and rebuild paths through one shared function.
Build commands explicitly target the configured Unix socket and its default builder. Docker API calls
use version 1.44, matching the existing setup minimum; the actual Engine 29.7.2 accepted health and
resource inventory through that endpoint. Inventory reuses its fetched container/disk data, eliminating
two redundant API calls per refresh. Build output retains only a bounded diagnostic tail in memory.
All five audit images were rebuilt with the current runner and revised role packs, and their recorded
content hashes matched. A separate git-bridge build verified the explicit socket path.

Actionlint and its ShellCheck integration found a checksum glob that could interpret filenames as flags;
the release script now uses `./*`. No release was published. Root frozen installation and audit passed
with 367 audited packages and no reported vulnerabilities. Both static analysis and actual packaging
are required evidence: neither alone established the renderer or unlabelled-image behavior above.

- [PixiJS v8 compatibility imports](https://pixijs.com/8.x/guides/migrations/v8).
- [PixiJS parser ID migration](https://pixijs.com/blog/8.12.0).
- [Docker Engine API compatibility](https://docs.docker.com/reference/api/engine/).
- [Oxc intentional side-effect imports](https://oxc.rs/docs/guide/usage/linter/rules/import/no-unassigned-import.html).

## Documentation and role guidance

README, architecture, stack, conventions, plan, agent instructions and art contracts were reconciled with
the running code. The original plan is preserved as explicitly historical material, including its old
acceptance claims. The art guides now agree on 48 × 120 character canvases, reception floor textures,
door approach behavior and persistent room labels. Local Markdown links were checked. Original sprite
sources, inbox images and the approved reference were preserved.

All vendored role guides were read and adapted. Removed material included unrelated framework/cloud
examples, absent researcher/tools, fixed context percentages and coverage quotas, automatic publishing,
and a Plankton workflow that could launch separately billed models outside HO's accounting. The retained
11 concise guides follow the task's requirements and HO's real reporting/handoff interfaces. Upstream
attribution and MIT licensing remain; VENDOR documents the adaptations. Shared short guides remain in
two self-contained role packs intentionally. No new provider subscription or additional agent framework
was introduced.

## Coverage and decisions

| Requested area                                  | Audit result and decision                                                                                                                                                                                                           |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| All source packages and applications            | Domain, protocol, persistence, daemon services, adapters, runner, CLI, desktop, UI and simulation reviewed; existing ACP spike also read and executed                                                                               |
| Configuration and Bun                           | Root/workspace manifests, lockfiles, compiler/linter/formatter/Knip, Bun isolated linking, scripts, hooks, ignores and devkit configuration reviewed                                                                                |
| GitHub Actions CI/CD                            | Pinned action SHAs, least-privilege jobs, immutable installs, audits and executable builds; tag validation and separate publishing job; local Actionlint passes                                                                     |
| Markdown clarity and currency                   | Current implementation separated from historical plans; setup, art and agent guidance corrected                                                                                                                                     |
| Structure, architecture, naming and readability | Lifecycle/resource ownership made explicit; ACP prompt handling, daemon launch and restart recovery separated; task branches use immutable IDs; no blanket directory rename needed                                                  |
| Refactoring and deduplication                   | Sharp replaces the PNG codec, TinyQueue replaces queue scans, proper-lockfile replaces homegrown locks, TanStack Query replaces repeated query effects; shared Docker image inspection and inventory data                           |
| CLI and UI flows                                | Input/path/ambiguity checks, connection deadlines, accurate failures, draft handling, floor state, query cancellation, credential labels and visible renderer failures                                                              |
| Framework and engine alternatives               | Keep React/PixiJS/Electrobun for current needs; Phaser, Tauri/Electron and server-rendered frameworks would impose migration costs without resolving the observed domain/lifecycle bugs; criteria are in STACK                      |
| Game mechanics, protocols and performance       | Fixed simulation steps, heap pathfinding, reachability flood fill, reservation cleanup and queued mail; bounded streams, reconnect cleanup, renderer cache and visible-actor work                                                   |
| Sprites and build                               | Validated Sharp imports, deterministic manifest revision, atomic UI output, preserved original art and regenerated build icons; atlas packing remains a measured future option                                                      |
| RAM and CPU                                     | Bound queues/log tails/render caches and reduce redundant requests/hidden work; no unsupported percentage or long-duration stability claim                                                                                          |
| Security                                        | Credential storage, loopback/CSP/origin checks, path containment, Docker hardening, project ownership, git publication and shutdown repairs; remaining boundaries listed below                                                      |
| Docker and external services                    | Pinned supported bases/toolchain, narrow contexts, locked npm trees, all provider targets built; configured socket consistency and reduced inventory requests                                                                       |
| AI model quality and cost                       | Current provider suggestions and reasoning configuration, bounded lifecycles, reduced skill/tool overhead and RTK hook verification; actual model quality/cost across paid runs remains unmeasured                                  |
| Strict TypeScript and useful tooling            | TS 7 strict root checks retained, React/accessibility/type-aware lint strengthened, Knip CSS ownership fixed, catalog/toolchain pins and Dependabot coverage updated; desktop devkit source requires documented compiler exceptions |
| Dead code and unnecessary complexity            | Removed custom PNG codec, duplicate image hash adapter, generated tracked icons and irrelevant skill material; Knip passes; original sprite files retained                                                                          |
| Database and event correctness                  | Serialized commands, idempotent replay, transactional append/report ordering and recovery; SQLite remains suitable, long-term snapshot/retention work is explicit                                                                   |

## Remaining limits and follow-up

The audit addresses the listed areas; it does not turn every planned feature into an implemented one.
The current prioritized follow-up list is in [PLAN.md](../PLAN.md): durable GitHub acknowledgement outbox,
long-term event/read-model/log retention, complete provider accounting and task-level budget semantics,
volume retention by last use, egress policy, sustained load/accessibility, missing art, remote TLS and
platform signing/expansion. These limits are described as such in the architecture instead of being
represented as finished functionality.

There were no new tests or code comments, no live credential-based provider comparisons, no destructive
migration of the owner's state and no fabricated performance or token-savings figures. Existing static
checks, manual isolated exercises, the existing ACP spike, browser observation, Docker builds and desktop
packaging form the verification evidence for this PR.
